МОДУЛЬ 1: РЕЛЯЦИОННАЯ МОДЕЛЬ И ОСНОВЫ SQL (DDL, DML) 1.1. Реляционная теория, таблицы, типы данных PostgreSQL и первичные ключи (PK). 1.2. Выборка и фильтрация данных: SELECT, WHERE, ORDER BY, LIMIT, OFFSET, NULL-логика. 1.3. Модификация данных: INSERT, UPDATE, DELETE, RETURNING, TRUNCATE. 1.4. Глоссарий, Кейс (Проектирование схемы каталога товаров) и Вопросы для самопроверки. МОДУЛЬ 2: СВЯЗИ, ВНЕШНИЕ КЛЮЧИ И СВЯЗЫВАНИЕ ТАБЛИЦ (JOINS) 2.1. Связи (1:1, 1:N, N:M), Внешние ключи (FK) и каскадные действия (ON DELETE). 2.2. Соединения: INNER JOIN, LEFT/RIGHT JOIN, FULL JOIN, CROSS JOIN, Self-Join. 2.3. Операции над множествами: UNION, INTERSECT, EXCEPT, фильтрация строк при JOIN. 2.4. Глоссарий, Кейс (Сборка сложного финансового отчёта из 5+ таблиц) и Вопросы для самопроверки. МОДУЛЬ 3: АГРЕГАЦИЯ, ГРУППИРОВКА И АНАЛИТИКА 3.1. Агрегатные функции (SUM, AVG, COUNT, MIN, MAX) и обработка NULL. 3.2. Группировка: GROUP BY, HAVING, GROUPING SETS, CUBE, ROLLUP. 3.3. Логика условных выражений (CASE WHEN) и фильтрация агрегатов (FILTER). 3.4. Глоссарий, Кейс (Расчет метрик e-commerce: ARPU, Retention, средний чек) и Вопросы для самопроверки. МОДУЛЬ 4: ВЛОЖЕННЫЕ ЗАПРОСЫ И CTE (COMMON TABLE EXPRESSIONS) 4.1. Подзапросы: Скалярные, подзапросы в FROM/WHERE (IN, EXISTS, ANY, ALL). 4.2. Табличные выражения: Синтаксис WITH (CTE), читаемость и разделение логики. 4.3. Рекурсивное CTE (WITH RECURSIVE): Иерархии, деревья и графы. 4.4. Глоссарий, Кейс (Построение дерева категорий и оргструктуры) и Вопросы для самопроверки. МОДУЛЬ 5: ОКОННЫЕ ФУНКЦИИ (WINDOW FUNCTIONS) 5.1. Концепция OVER(), подмножества PARTITION BY, сортировка ORDER BY и фреймы (ROWS/RANGE). 5.2. Ранжирующие функции: ROW_NUMBER(), RANK(), DENSE_RANK(), NTILE(). 5.3. Смещение и агрегаты: LAG(), LEAD(), FIRST_VALUE(), накопительный итог (Running Total). 5.4. Глоссарий, Кейс (Расчет скользящего среднего и ранжирование клиентов) и Вопросы для самопроверки. МОДУЛЬ 6: ПРОЕКТИРОВАНИЕ БД, НОРМАЛИЗАЦИЯ И JSONB 6.1. Нормализация (1NF, 2NF, 3NF, BCNF) vs Денормализация для OLTP/OLAP. 6.2. Работа с полуструктурированными данными: JSONB, JSON-операторы (->, ->>, @>), GIN-индексы. 6.3. Массивы, Enum, Custom-типы данных в PostgreSQL. 6.4. Глоссарий, Кейс (Гибридная схема: Реляционный core + JSONB атрибуты) и Вопросы для самопроверки. МОДУЛЬ 7: ТРАНЗАКЦИИ, БЛОКИРОВКИ И ИЗОЛЯЦИЯ (ACID) 7.1. Гарантии ACID, управление транзакциями: BEGIN, COMMIT, ROLLBACK, SAVEPOINT. 7.2. Уровни изоляции транзакций (Read Committed, Repeatable Read, Serializable) и аномалии. 7.3. Явные и неявные блокировки: SELECT FOR UPDATE, FOR SHARE, Deadlocks. 7.4. Глоссарий, Кейс (Проектирование безопасной системы списания баланса) и Вопросы для самопроверки. МОДУЛЬ 8: ИНДЕКСАЦИЯ И ОПТИМИЗАЦИЯ ПРОИЗВОДИТЕЛЬНОСТИ 8.1. Внутреннее устройство индексов: B-Tree, Hash, GIN, GiST, BRIN. 8.2. Стратегии индексации: Составные (Composite), Покрывающие (INCLUDE), Частичные (Partial) индексы. 8.3. Профилирование запросов: EXPLAIN ANALYZE, интерпретация узлов (Seq Scan, Index Scan, Bitmap Scan). 8.4. Глоссарий, Кейс (Ускорение медленного запроса в 100 раз через EXPLAIN) и Вопросы для самопроверки. МОДУЛЬ 9: ПРОДВИНУТЫЙ POSTGRESQL (ПРОЦЕДУРЫ, ТРИГГЕРЫ, ВЬЮ) 9.1. Представления (Views) и Материализованные представления (Materialized Views, REFRESH). 9.2. Хранимые функции и процедуры на PL/pgSQL: Переменные, ветвление, циклы. 9.3. Триггеры (Triggers): BEFORE, AFTER, INSTEAD OF, обработка изменений (OLD/NEW). 9.4. Глоссарий, Кейс (Автоматический аудит изменений таблиц via Triggers) и Вопросы для самопроверки. МОДУЛЬ 10: АРХИТЕКТУРА И PRODUCTION (MIGRATIONS, POOLING, MAINTENANCE) 10.1. Эволюция схемы: Миграции (Alembic / Goose), безопасный накат изменений (Zero Downtime DDL). 10.2. Управление подключениями: PgBouncer, Connection Pooling, клиенты Asyncpg/SQLAlchemy. 10.3. Обслуживание PostgreSQL: VACUUM, ANALYZE, Autovacuum, репликация, резервное копирование (pg_dump, WAL). 10.4. Глоссарий, Кейс (Полный развертываемый стенд PostgreSQL + PgBouncer в Docker) и Вопросы для самопроверки.